<p id="notice"></p>
<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <%= render 'books/book', user: @user, book: @book %>
    </div>

    <div class="col-md-8 offset-md-1">
      <h2>Books</h2>
      <%= render 'books/index', books: @books %>
      <%= render 'daily_posts_form', user: @user %>
      <%= render 'post_count', books: @books %>
    </div>
  </div>

</div>

